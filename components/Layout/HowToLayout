1 Создаем базовый Layout (обычный компонент, разметка которого общая для всех страниц)
2 Создаем функцию - ХОК - принимает компонент(страницу), возвращаем эту же страничку но обернутую в наш созданный Layout
; export const getLayout = (page: ReactElement) => {
;     return <Layout>{page}</Layout>
; }
3 Сюда можем пихать провайдеры
4 В _app есть строка которая проверяет есть ли лэйаут в компоненте , или нет
  const getLayout = Component.getLayout ?? ((page) => page);